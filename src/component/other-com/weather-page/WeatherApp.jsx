import ThemeWrapper from "./ThemeWrapper";
import WeatherCard from "./WeatherCard";

const WeatherApp = () => {
	return (
		<ThemeWrapper>
			<WeatherCard />
		</ThemeWrapper>
	);
};

export default WeatherApp;
